# GitHub presence — paste-ready copy

## Organization (github.com/BinacciAI)

**Display name:** Binacci AI

**Org bio / About (short, 1 line):**
> Deterministic, auditable crypto trading agents. The strategy decides; the AI only executes. CMC signals · Trust Wallet self-custody · BNB Chain.

**Pinned README (org profile `BinacciAI/.github` → `profile/README.md`):**
> ### Binacci AI
> AI trading agents hallucinate trades or blow up. **Binacci separates brains from hands** — a deterministic, auditable strategy engine decides; the AI only executes.
> - 🧠 5-strategy portfolio over 50+ BNB markets, every entry passes a multi-gate audit
> - 🔒 Trust Wallet self-custody — keys sign locally, the agent never holds them
> - 📊 Built on CoinMarketCap signals + the BNB AI Agent SDK
> - 🧪 Track-2 Strategy Skills: backtestable specs from CMC data
>
> Built for the BNB Chain × CoinMarketCap × Trust Wallet AI Agent Hackathon.
> 🌐 binacci.ai · 𝕏 @binacciai

## Repository: trading-agent

**Description (the "About" field, ≤ 350 chars):**
> Autonomous BNB Chain trading agent. A deterministic risk engine runs a 5-strategy portfolio over 50+ markets on CoinMarketCap signals and executes via Trust Wallet self-custody — the AI is an executor, never a decider. Includes Track-2 CMC Strategy Skills (backtestable specs).

**Website field:** https://binacci.ai

**Topics (add these tags):**
`bnb-chain` · `binance-smart-chain` · `defi` · `trading-bot` · `ai-agent` · `coinmarketcap` · `trust-wallet` · `pancakeswap` · `algorithmic-trading` · `quant` · `erc-8004` · `erc-8183` · `x402` · `python` · `nextjs` · `crypto`

**Repo README:** see top-level `README.md` (rewritten for v0.2 — portfolio, 50+ markets, risk modes, skill family).

## Suggested repo hygiene before submission
- Add a `LICENSE` (the framework is published for evaluation; note proprietary params).
- Pin `trading-agent` (and `strategy-core` if you want it visible) on the org profile.
- Add screenshots of the dashboard Command Center + Strategies pages to the README.
- Turn on branch protection on `main` and use PRs for larger changes going forward.
